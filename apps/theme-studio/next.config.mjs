/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ['@mui/material', '@mui/material-nextjs', '@mui/icons-material'],
  reactStrictMode: true,
};

export default nextConfig;
